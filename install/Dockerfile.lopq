FROM ann-benchmarks

RUN apt-get install -y python-numpy python-pip
RUN git clone https://github.com/yahoo/lopq.git
RUN cd lopq/python && pip install .

# lopq doesn't support python3 for now.
RUN python -c 'import lopq'

ENTRYPOINT ["python", "run_algorithm.py"]